// Example with arrow functions and function calls
const banane = 'bob';

const add = (a, b) => a + b;
const multiply = (x, y) => x * y;

const total = add(40, 35);
const product = multiply(5, 6);

let result = 0;
if (total > 50) {
    result = product;
}
